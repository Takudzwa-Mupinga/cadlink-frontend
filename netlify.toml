[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: let the client-side router handle all routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
